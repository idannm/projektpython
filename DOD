
# ✅ שלב 1 — Working CLI (הוכחה שהכל עובד)
## EC2 Constraints

### ✔ רק t3.micro או t2.small

תריץ:

```bash
python main.py ec2 create --name test1 --type t3.micro
```

ואז תנסה בכוונה משהו אסור:

```bash
python main.py ec2 create --name bad --type t3.large
```

בפלט חייב להיות משהו כמו:

❌ Instance type not allowed

---

## ✔ Hard Cap של 2 Instances Running

תריץ פעמיים:

```bash
python main.py ec2 create --name server1 --type t3.micro
python main.py ec2 create --name server2 --type t3.micro
```

עכשיו נסה שלישי:

```bash
python main.py ec2 create --name server3 --type t3.micro
```

חייב לקבל:

❌ Cannot create more than 2 running instances

📌 זה ההוכחה הכי חשובה

---

## ✔ Start/Stop רק Instances עם Tags

תריץ:

```bash
python main.py ec2 list
```

תיקח Instance ID שמופיע

ואז:

```bash
python main.py ec2 stop i-xxxxxxxx
python main.py ec2 start i-xxxxxxxx
```

אם תנסה Instance שלא נוצר ע"י CLI → צריך לחסום.

---

---

# ✅ שלב 2 — Tagging (המרצה בודק את זה ראשון)

כל Resource חייב להכיל Tags:

* CreatedBy=platform-cli
* Owner=<username>
* Project=CloudAutomation
* Environment=Dev

📌 אתה חייב להוסיף את זה בקוד שלך.

ב־EC2:

```python
Tags=[
    {"Key": "CreatedBy", "Value": "platform-cli"},
    {"Key": "Owner", "Value": owner},
    {"Key": "Project", "Value": "CloudAutomation"},
    {"Key": "Environment", "Value": "Dev"},
]
```

ב־S3:

```python
s3_client.put_bucket_tagging(
    Bucket=name,
    Tagging={
        "TagSet": [
            {"Key": "CreatedBy", "Value": "platform-cli"},
            {"Key": "Owner", "Value": owner},
        ]
    }
)
```

ב־Route53 Hosted Zone:

```python
r53_client.change_tags_for_resource(
    ResourceType="hostedzone",
    ResourceId=zone_id,
    AddTags=[
        {"Key": "CreatedBy", "Value": "platform-cli"},
        {"Key": "Owner", "Value": owner},
    ]
)
```

---

### איך להוכיח שזה באמת שם?

תעשה צילום מה־AWS Console או תריץ CLI:

```bash
aws ec2 describe-tags --filters Name=resource-id,Values=i-xxxx
```

---

---

# ✅ שלב 3 — Security (חובה!)

המרצה רוצה לראות:

## ❌ אין Secrets בתוך Git

תוודא שאין בקוד:

* Access Key
* Secret Key
* Tokens

אתה חייב לעבוד עם:

```bash
aws configure
```

או profile:

```bash
python main.py --profile default ec2 list
```

---

## `.gitignore` חובה

תיצור קובץ:

```gitignore
.env
__pycache__/
*.pem
venv/
```

---

---

# ✅ שלב 4 — Demo Evidence (הכי חשוב להגשה)

עכשיו אנחנו מכינים קטע שאתה מדביק ל־README.

## 📌 EC2 Demo

```bash
# Create instance
python main.py ec2 create --name MyServer --type t3.micro

# List instances
python main.py ec2 list

# Stop instance
python main.py ec2 stop i-05d50057c7ebad7c6

# Start instance
python main.py ec2 start i-05d50057c7ebad7c6
```

---

## 📌 S3 Demo

```bash
# Create bucket
python main.py s3 create --name idan-demo-bucket --public

# List buckets
python main.py s3 list

# Upload file
python main.py s3 upload --bucket idan-demo-bucket --file test.txt
```

---

## 📌 Route53 Demo

```bash
# Create hosted zone
python main.py r53 create --name eexample.com

# List zones
python main.py r53 list
```

---

## DNS Record Evidence

בגלל שאין לך create-record ב־CLI, אתה מוכיח ידנית:

```bash
nslookup www.eexample.com
```

---

# ✅ שלב 5 — README Final Section

בסוף ה־README תוסיף:

```md
## Demo Evidence

EC2, S3, and Route53 resources were successfully created and managed using the CLI.

- EC2 create/list/start/stop ✔
- S3 create/list/upload ✔
- Route53 hosted zone + DNS record ✔


✅ להדביק פה את `main.py` שלך
או לפחות את החלק של:

* ec2 create
* s3 create
* r53 create

ואני אתקן לך אותו כך שיהיה:

✔ Tags מושלמים
✔ Constraints עובדים
✔ Route53 records גם יהיו קיימים
✔ בלי שום באגים

שלח את הקובץ עכשיו ונעשה את זה סופי להגשה.
